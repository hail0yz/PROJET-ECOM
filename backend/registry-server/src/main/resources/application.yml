spring:
  application.name: register-server
  security:
    user:
      name: eureka
      password: eureka

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

server:
  port: 8761
